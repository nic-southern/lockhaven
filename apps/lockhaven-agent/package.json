{
  "name": "@nms/lockhaven-agent",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "build": "CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o dist/lockhaven-agent ./cmd/lockhaven-agent",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "typecheck": "go test -count=0 ./..."
  }
}
